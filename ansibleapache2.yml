---
- hosts : all
     become: yes
     tasks : 
        -name: Install httpd
         apt : name=httpd state=present
        -name : Start httpd
         service: name=httpd state:started
         
        
